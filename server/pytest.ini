# Collection roots for the whole server suite. Hooks and docs invoke bare
# `pytest` so newly added test files are picked up automatically instead of
# silently skipped by an explicit file list.
[pytest]
testpaths = test_server.py analysis
